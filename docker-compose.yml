version: '3.6'
services:
    deemix:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ./Downloads:/root/Music/deemix Music
        ports:
            - 6595:6595
